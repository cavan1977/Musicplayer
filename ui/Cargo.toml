[package]
name = "ui"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
dioxus = { version = "0.5", features = ["web", "desktop"] }
dioxus-router = "0.5"
serde = { version = "1.0", features = ["derive"] }
